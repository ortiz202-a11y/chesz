# Gradle
.gradle/
build/
**/build/

# Android Studio / IntelliJ (aunque no lo usemos)
.idea/
*.iml

# Local properties (nunca al repo)
local.properties

# OS
.DS_Store

# Build artifacts / downloaded CI artifacts
chesz-debug-apk/
*.apk

# Stockfish binario (se inyecta en GitHub Actions, no se sube al repo)
app/src/main/assets/stockfish/stockfish-arm64

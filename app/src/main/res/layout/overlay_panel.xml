<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/panelRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#66000000">

    <!-- CARD PRINCIPAL (tamaño se define por código usando % de pantalla) -->
    <LinearLayout
        android:id="@+id/panelCard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="#CC000000">

        <!-- Área de RESULTADO (único contenido visible para usuario final) -->
        <TextView
            android:id="@+id/resultText"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text=""
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:lineSpacingExtra="4dp" />

        <View
            android:layout_width="match_parent"
            android:layout_height="12dp" />

        <!-- Selector de MODO (W / L) - estilo chip -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center">

            <TextView
                android:id="@+id/modeW"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_weight="1"
                android:gravity="center"
                android:text="W"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:background="#FF222222" />

            <View
                android:layout_width="8dp"
                android:layout_height="match_parent" />

            <TextView
                android:id="@+id/modeL"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_weight="1"
                android:gravity="center"
                android:text="L"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:background="#99000000" />
        </LinearLayout>

        <!-- DEBUG (apagado por defecto) -->
        <TextView
            android:id="@+id/debugText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textSize="12sp"
            android:textColor="#AAFFFFFF"
            android:visibility="gone"
            android:text="" />
    </LinearLayout>

</FrameLayout>

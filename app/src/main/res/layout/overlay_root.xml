<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <FrameLayout
        android:id="@+id/bubbleContainer"
        android:layout_width="55dp"
        android:layout_height="55dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:background="@android:drawable/screen_background_dark_transparent">
        
        <ImageView
            android:id="@+id/bubbleIcon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_analyzer" />
    </FrameLayout>

    <LinearLayout
        android:id="@+id/panelBubble"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:layout_above="@id/bubbleContainer"
        android:layout_alignStart="@id/bubbleContainer"
        android:background="#DD000000"
        android:orientation="vertical"
        android:padding="8dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/stateText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Sshot / Fen / Ai / Done"
            android:textColor="#AAAAAA"
            android:textSize="10sp" />

        <TextView
            android:id="@+id/apiDataText"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_marginTop="5dp"
            android:text="Esperando datos de API..."
            android:textColor="#FFFFFF" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="8dp"
            android:background="#444444" />

        <TextView
            android:id="@+id/btnClosePanel"
            android:layout_width="match_parent"
            android:gravity="center"
            android:padding="8dp"
            android:text="CERRAR PANEL"
            android:textColor="#FF4444"
            android:textStyle="bold" />
    </LinearLayout>

</RelativeLayout>
